const name = "sohail"
const repoCount = " 50"

//console.log(name+repoCount+"value");  // old syntax preffer to use new syntax

console.log(`My name is ${name} and i have ${repoCount} repositries on github`); // it is good to use

const channelName = new String("sohail-com-jampur")

console.log(channelName.split("-")); // for trim anithing

let account = "    sohail    "
console.log(account);
console.log(account.trim(""));  // trim white spaces

console.log(account.replace("s", "MS")); // replace anything in stirngs

console.log(account.indexOf("o"))
console.log(account.charAt(5));  // its answer is o because of whitespaces






 